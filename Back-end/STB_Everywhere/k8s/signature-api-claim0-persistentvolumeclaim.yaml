apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: signature-api-claim0
  name: signature-api-claim0
spec:
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 100Mi
